spring:
  application:
    name: auth-service
  datasource:
    username: security
    password: postgres
    url: jdbc:postgresql://localhost:5432/airline-security
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update